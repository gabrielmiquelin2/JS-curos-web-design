//Antes de vir o DOM (que é a manipulação do html em js) tinha BOM:

//BOM  "Browser Object Model" (Modelo de Objetos do Navegador) permite aos desenvolvedores interagir com a janela do navegador

innerHeight //  retorna a altura da área de visualização da janela do navegador
innerWidth //retorna a largura da área de visualização da janela do navegador
screenX //Posição da janela em relação ao munitor

// location: representa a localização atual do navegador (ou seja, a URL atual). Alguns dos métodos do objeto location incluem:
//  location.href  retorna a URL completa atual.
// location.reload(): recarrega a página atual.
// location.assign(url): carrega uma nova página no navegador com a URL especificada.
// location.replace(url): substitui a página atual no histórico do navegador pela página com a URL especificada.

// history: representa o histórico de navegação do usuário. Alguns dos métodos do objeto history incluem:
// history.back(): move o navegador para a página anterior no histórico.
// history.forward(): move o navegador para a próxima página no histórico.
// history.go(delta): move o navegador para a página delta no histórico, onde delta pode ser positivo ou negativo.

window.open("https://github.com/gabrielmiquelin2")//colocando no console ele apre esse link assim que eu der enter

scrollBy(0, 50)//50 é a posição, basta eu copiar e colar esse code vairas vezes ele vai scrolando

